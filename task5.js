// 5. True or False Practice
console.log("\n--- Task 5 ---");
const votingAge = 18;
const currentAge = 25;
const isOldEnoughToVote = currentAge >= votingAge;
const hasId = true;

console.log(`Is old enough to vote? ${isOldEnoughToVote}`);
console.log(`Can vote today? ${isOldEnoughToVote && hasId}`);
